<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:contest="http://www.springframework.org/schema/context"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context-3.1.xsd
                        ">

	<!-- 导入外部文件mail.properties -->
	<contest:property-placeholder location="classpath:config/mail.properties" />

	<!--<bean id="propertyConfigurer" class="org.springframework.mail.javamail.JavaMailSenderImpl">-->
	<!--<property name="host" value="${mail.smtp.host}" />-->
	<!--<property name="username" value="${mail.smtp.username}" />-->
	<!--<property name="password" value="${mail.smtp.password}" />-->
	<!--<property name="javaMailProperties">-->
	<!--<props>-->
	<!--&lt;!&ndash; 必须进行授权谁，阻止他人任意乱发邮件 &ndash;&gt;-->
	<!--<prop key="mail.smtp.auth">${mail.smtp.auth}</prop>-->
	<!--<prop key="mail.stmp.port">${mail.stmp.port}</prop>-->
	<!--<prop key="mail.smtp.timeout">${mail.smtp.timeout}</prop>-->
	<!--</props>-->
	<!--</property>-->

	<!--</bean>-->


	<!-- beans存放了很多个类，把一个类放入到spring容器中，该类就是bean
         一个bean就是描述一个类，id就是标示符
         命名规范：类的第一个字母变成小写，其他的字母保持不变，class为类的全名
     -->
</beans>